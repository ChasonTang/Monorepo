Write an RFC for the requirement below using docs/rfc_000_design_doc_template.md. Treat every rule in that template as a hard constraint.

<requirement>
</requirement>

Save to docs/rfc_NNN_{slug}.md, where NNN is the lowest unused three-digit index in docs/.
